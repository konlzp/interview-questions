// Assume you have a function to check if substring.
// Call this function only once to determine if a function is a rotation of the other

class isRotation{
    static isRotation(str1, str2) {
        
    }
}